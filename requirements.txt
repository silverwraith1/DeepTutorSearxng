# ============================================
# DeepTutor Python Dependencies - STABLE FLAT BUILD
# ============================================

# 1. THE HEAVYWEIGHTS (Install these first to anchor the build)
llama-index
docling
raganything
langchain-community
langchain-core

# 2. CORE UTILS (Pinned to prevent conflicts)
pydantic>=2.0.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
pillow<13.0.0

# 3. HTTP & API
openai>=1.30.0
anthropic>=0.30.0
requests>=2.32.2
aiohttp>=3.9.4
httpx>=0.27.0
urllib3>=2.2.1
python-dotenv>=1.0.0

# 4. WEB FRAMEWORK
fastapi>=0.100.0
uvicorn[standard]>=0.24.0
websockets>=12.0
python-multipart>=0.0.6

# 5. TOOLS & MISC
PyYAML>=6.0
tiktoken>=0.5.0
jinja2>=3.1.0
nest_asyncio>=1.5.8
tenacity>=8.0.0
arxiv>=2.0.0
PyMuPDF>=1.26.0

# 6. DEV & TEST (Simplified)
pytest>=7.0.0
pre-commit>=3.0.0
bandit>=1.8.0
safety<3.0.0
